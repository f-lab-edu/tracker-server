export interface PageInfoRefer {
  domain: string;
  userId: string;
  referrer: string;
  url: string;
}
export interface PageInfo {
  domain: string;
  userId: string;
  url: string;
}
export interface PageViewCountInfo {
  domain: string;
  url: string;
  date: string;
}
